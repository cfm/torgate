# port to operate http on
port = $PORT

# the tld attached to the onion urls
tld = "$TLD"

# the config for the tor socks proxy
proxyHost = "127.0.0.1"
proxyPort = 9050

# config for client
userAgent = "Torgate/0.1 (Unix; Linux)"
verboseClient = true

# config for server
serverBanner = "Torgate/0.1"
localPath = "/var/torgate/html"
